---
import Post from "../../../../../../layouts/Post.astro";
import { getEntryBySlug } from "astro:content";

const entry = await getEntryBySlug("blog", "infrasoundbed");
if (!entry) throw new Error("Blog entry not found: infrasoundbed");

// Render markdown properly (don’t rely on post.rendered)
const { Content } = await entry.render();
---

<Post frontmatter={entry.data} slug={entry.slug}>
  <Content />
</Post>
